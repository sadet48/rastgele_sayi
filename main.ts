let sayi = 0
input.onGesture(Gesture.Shake, function () {
    sayi = randint(0, 3)
    if (sayi == 1) {
        basic.showString("" + (sayi))
    } else if (sayi == 1) {
        basic.showString("" + (sayi))
    } else {
        basic.showString("" + (sayi))
    }
})
